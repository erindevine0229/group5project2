const createChat = document.getElementById(createChat);
const userLogin = document.getElementById(userLogin);
const createAProfile = document.getElementById(createAProfile);

// userLogin todo's

// when create a user is clicked it will give them a prompt to enter in their name, age, username, password, password confirm

// when user login is clicked they are prompted to enter in a username and password

// we want the users info to be safe so store it in the backend somewhere somewhere something and each user is a array or object

// the users can add a pfp

// and this is available on homepage and on potential blog page
