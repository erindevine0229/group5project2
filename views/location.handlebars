<section class="location">
  <img src="/images/{{location.filename}}" alt="{{location.description}}">
  <div class="location-details">
    <h3>{{location.title}}</h3>
    <p>By: {{location.artist}}</p>
  </div>
</section>